[package]
name = "templates-example"
version = "0.1.1"
edition = "2024"
publish = false

[dependencies]
modo = { workspace = true, features = ["templates"] }
chrono = "0.4"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("templates"))'] }
