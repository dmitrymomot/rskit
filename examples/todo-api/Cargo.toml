[package]
name = "todo-api"
version = "0.1.2"
edition = "2024"
publish = false

[dependencies]
modo.workspace = true
modo-db = { workspace = true, features = ["sqlite"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
sentry = ["modo/sentry"]
