name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            rust:
              - '**/*.rs'
              - 'Cargo.toml'

  fmt:
    name: Format
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --check

  lint:
    name: Lint
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --features full --tests -- -D warnings
      - run: cargo clippy --no-default-features --features ldb --tests -- -D warnings

  test:
    name: Test
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --features full

  test-minimal:
    name: Test (default features)
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo check
      - run: cargo test

  test-features:
    name: Test (${{ matrix.feature }})
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        feature: [auth, templates, sse, email, storage, webhooks, dns, geolocation, sentry, test-helpers, ldb]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Check
        if: matrix.feature != 'ldb'
        run: cargo check --features ${{ matrix.feature }}
      - name: Check (ldb, no default features)
        if: matrix.feature == 'ldb'
        run: cargo check --no-default-features --features ldb
      - name: Test (ldb)
        if: matrix.feature == 'ldb'
        run: cargo test --no-default-features --features ldb

  publish:
    name: Publish to crates.io
    needs: [changes, fmt, lint, test, test-minimal, test-features]
    if: >-
      always()
      && github.event_name == 'push'
      && github.ref == 'refs/heads/main'
      && needs.changes.outputs.rust == 'true'
      && !contains(needs.*.result, 'failure')
      && !contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check if version changed
        id: version
        run: |
          LOCAL=$(grep '^version' Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          EXISTS=$(curl -sf "https://crates.io/api/v1/crates/modo-rs/versions" \
            | python3 -c "
          import sys, json
          versions = [v['num'] for v in json.load(sys.stdin)['versions']]
          print('true' if '$LOCAL' in versions else 'false')
          " 2>/dev/null) || EXISTS="false"
          if [ "$EXISTS" = "true" ]; then
            echo "Version $LOCAL already published, skipping."
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "Version $LOCAL not found on crates.io, will publish."
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - uses: dtolnay/rust-toolchain@stable
        if: steps.version.outputs.changed == 'true'
      - uses: Swatinem/rust-cache@v2
        if: steps.version.outputs.changed == 'true'

      - name: Publish modo
        if: steps.version.outputs.changed == 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish
