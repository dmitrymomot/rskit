[package]
name = "{{ project_name }}"
version = "0.1.0"
edition = "2024"

[dependencies]
modo = "0.1"
modo-db = { version = "0.1", features = ["{{ db_driver }}"] }
modo-jobs = "0.1"
serde = { version = "1", features = ["derive"] }
